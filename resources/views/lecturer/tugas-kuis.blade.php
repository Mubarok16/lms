@extends('lecturer.app-lecturer') @section('ketjudul','Pembelajaran') @section('judul','Tugas & Kuis') @section('content')<div class="grid lg:grid-cols-2 gap-6"><section><h2 class="font-display text-xl font-semibold mb-3">Tugas</h2><div class="space-y-3">@forelse($tugas as $t)<a href="{{ route('lecturer.tugas.show',$t) }}" class="block rounded-2xl border border-line bg-white p-5 hover:shadow-md"><div class="flex justify-between"><h3 class="font-semibold">{{ $t->judul }}</h3><span class="text-xs font-mono text-ink/45">{{ $t->deadline?->format('d M Y H:i') ?: 'Tanpa deadline' }}</span></div><p class="mt-1 text-sm text-ink/50">{{ $t->pengajaranDosen->kelas->matakuliah->nama_mk }} · Kelas {{ $t->pengajaranDosen->kelas->kode_kelas }}</p></a>@empty<div class="rounded-2xl border border-dashed border-line p-10 text-center text-ink/45">Belum ada tugas.</div>@endforelse</div></section><section><h2 class="font-display text-xl font-semibold mb-3">Kuis</h2><div class="space-y-3">@forelse($quiz as $q)<a href="{{ route('lecturer.quiz.show',$q) }}" class="block rounded-2xl border border-line bg-white p-5 hover:shadow-md"><div class="flex justify-between"><h3 class="font-semibold">{{ $q->judul }}</h3><span class="text-xs rounded-full px-2 py-1 {{ $q->is_published?'bg-teal/10 text-teal':'bg-paper text-ink/50' }}">{{ $q->is_published?'Terbit':'Draft' }}</span></div><p class="mt-1 text-sm text-ink/50">{{ $q->pengajaranDosen->kelas->matakuliah->nama_mk }}</p></a>@empty<div class="rounded-2xl border border-dashed border-line p-10 text-center text-ink/45">Belum ada kuis.</div>@endforelse</div></section></div>@endsection
