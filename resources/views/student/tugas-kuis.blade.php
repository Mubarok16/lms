@extends('student.app-student') @section('ketjudul','Pembelajaran') @section('judul','Tugas & Kuis') @section('content')<div class="grid lg:grid-cols-2 gap-6"><section><h2 class="font-display text-xl font-semibold mb-3">Tugas</h2><div class="space-y-3">@forelse($tugas as $t)<a href="{{ route('student.tugas.show',$t) }}" class="block rounded-2xl border border-line bg-white p-5 hover:shadow-md"><div class="flex justify-between"><h3 class="font-semibold">{{ $t->judul }}</h3><span class="text-xs font-mono text-ink/45">{{ $t->deadline?->format('d M Y H:i') ?: 'Tanpa deadline' }}</span></div><p class="mt-1 text-sm text-ink/50">{{ $t->pengajaranDosen->kelas->matakuliah->nama_mk }} · Kelas {{ $t->pengajaranDosen->kelas->kode_kelas }}</p></a>@empty<div class="rounded-2xl border border-dashed border-line p-10 text-center text-ink/45">Belum ada tugas.</div>@endforelse</div></section><section><h2 class="font-display text-xl font-semibold mb-3">Kuis</h2><div class="space-y-3">@forelse($quiz as $q)<a href="{{ route('student.quiz.show',$q) }}" class="block rounded-2xl border border-line bg-white p-5 hover:shadow-md"><div class="flex justify-between"><h3 class="font-semibold">{{ $q->judul }}</h3><span class="text-xs bg-teal/10 text-teal px-2 py-1 rounded-full">{{ $q->durasi_menit }} menit</span></div><p class="mt-1 text-sm text-ink/50">{{ $q->pengajaranDosen->kelas->matakuliah->nama_mk }}</p></a>@empty<div class="rounded-2xl border border-dashed border-line p-10 text-center text-ink/45">Belum ada kuis aktif.</div>@endforelse</div></section></div>@endsection
