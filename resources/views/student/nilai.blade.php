@extends('student.app-student') @section('ketjudul','Akademik') @section('judul','Nilai') @section('content')<div class="overflow-hidden rounded-2xl border border-line bg-white"><div class="p-5 border-b border-line"><h2 class="font-display text-lg font-semibold">Rekap Nilai</h2><p class="text-sm text-ink/50 mt-1">Nilai yang sudah diberikan oleh dosen.</p></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-paper"><tr><th class="p-4 text-left">Jenis</th><th class="p-4 text-left">Judul</th><th class="p-4 text-left">Mata Kuliah</th><th class="p-4 text-left">Nilai</th><th class="p-4 text-left">Waktu</th></tr></thead><tbody class="divide-y divide-line">@foreach($tugas as $x)<tr><td class="p-4">Tugas</td><td class="p-4">{{ $x->tugas->judul }}</td><td class="p-4">{{ $x->tugas->pengajaranDosen->kelas->matakuliah->nama_mk }}</td><td class="p-4 font-semibold">{{ $x->skor ?? 'Belum dinilai' }}</td><td class="p-4">{{ $x->dikoreksi_at?->format('d M Y H:i') ?? '-' }}</td></tr>@endforeach @foreach($quiz as $x)<tr><td class="p-4">Kuis</td><td class="p-4">{{ $x->quiz->judul }}</td><td class="p-4">{{ $x->quiz->pengajaranDosen->kelas->matakuliah->nama_mk }}</td><td class="p-4 font-semibold">{{ $x->skor ?? 'Belum dinilai' }}</td><td class="p-4">{{ $x->waktu_submit?->format('d M Y H:i') ?? '-' }}</td></tr>@endforeach @if($tugas->isEmpty() && $quiz->isEmpty())<tr><td colspan="5" class="p-12 text-center text-ink/45">Belum ada nilai.</td></tr>@endif</tbody></table></div></div>@endsection
